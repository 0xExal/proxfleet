# Proxmox Configuration
# Proxmox API URL (do not modify the port or path)
PROXMOX_ENDPOINT="https://your-proxmox.local:8006/api2/json"

# Proxmox API Credentials
# Create a dedicated user: pveum user add terraform@pve
# Create a role with necessary permissions
PROXMOX_USERNAME="terraform@pve"
PROXMOX_PASSWORD="your-secure-password"

# SSH username for advanced operations (cleanup, resize wait)
# Must have SSH access to the Proxmox node
PROXMOX_SSH_USERNAME="root"

# SSH public key for access to created VMs
# Generate with: ssh-keygen -t ed25519
SSH_PUBLIC_KEY="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... user@host"

# Encrypted password for cloud-init (optional)
# Generate with: mkpasswd -m sha-512
# If empty, only SSH key authentication will be available
CLOUDINIT_PASSWORD_CRYPTED=""
